# Exemplo simplificado de api/openapi.yaml
openapi: 3.0.0
info:
  title: SmartPort TOS API
  version: 1.0.0
paths:
  /ships/{id}:
    get:
      summary: Retorna o status de carga de um navio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string